<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme.TabsWithFragmentStateAdapter" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="AppTheme.Light.NoActionBar" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/colorRed</item>
        <item name="colorPrimaryDark">@color/colorStatusBar</item>
        <item name="colorSecondary">@color/colorTeal</item>
    </style>

    <style name="Theme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" /> <!--Previously ThemeOverlay.AppCompat.Dark.ActionBar -->

    <style name="Theme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" /> <!--Previously ThemeOverlay.AppCompat.Light -->

    <style name="Theme.TabStyle" parent="TextAppearance.AppCompat.Widget.ActionBar.Title"> <!--Can't find a replacement for the app compat style. -->
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/colorGray</item>
    </style>
</resources>